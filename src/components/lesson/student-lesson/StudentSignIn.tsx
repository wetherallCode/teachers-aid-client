import React, { FC } from 'react'
import { Modal } from '../../../animations/Modal'

export type StudentSignInProps = {}

export const StudentSignIn: FC<StudentSignInProps> = () => {
  return <>{/* <Modal isToggled={} setIsToggled={} /> */}</>
}
